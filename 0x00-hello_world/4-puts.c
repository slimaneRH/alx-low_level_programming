#include <stdio.h>
/**
 * main - A  Program which print a line using puts function.
 *  Return: Always 0 (indicating successful completion).
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
