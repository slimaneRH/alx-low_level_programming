#include <stdio.h>
/**
 * main - program that print a line using printf funcrion.
 * Return: Always 0 (indicating successful completion).
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art, \n")
	return (0);
	}

